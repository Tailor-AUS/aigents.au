/** Operator workspace for university-contained collective projects. */
const esc = value => String(value ?? '').replace(/[&<>"']/g, char => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[char]));
const money = value => new Intl.NumberFormat('en-AU', { style:'currency', currency:'AUD', maximumFractionDigits:0 }).format(Number(value || 0));
const fte = value => Number(value || 0).toFixed(2);
const statusLabel = status => ({ preparing:'Preparing', recruiting:'Recruiting', active:'Active', completed:'Completed', cancelled:'Cancelled' }[status] || status);
const outputLabel = status => ({ open:'Available', claimed:'Claimed', in_progress:'In progress', submitted:'Awaiting review', done:'Signed off' }[status] || String(status || 'Available').replace(/_/g, ' '));
function safeLink(value) { try { const url = new URL(String(value)); return ['https:','http:'].includes(url.protocol) && !url.username && !url.password ? url.href : ''; } catch { return ''; } }
function stamp(value) { const date = new Date(value); return Number.isFinite(date.getTime()) ? date.toLocaleString('en-AU', { day:'numeric', month:'short', year:'numeric', hour:'2-digit', minute:'2-digit' }) : ''; }
const css = `:root{--ink:#15243c;--muted:#637086;--blue:#2457e8;--canvas:#f5f7fb;--line:#e1e6ee;--lime:#d9f46b}*{box-sizing:border-box}body{margin:0;background:var(--canvas);color:var(--ink);font:15px/1.6 Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}a{color:var(--blue)}button,input,textarea,select{font:inherit}button{cursor:pointer}.wrap{width:min(1120px,calc(100% - 40px));margin:auto}header{background:#fff;border-bottom:1px solid var(--line)}.nav{min-height:78px;display:flex;justify-content:space-between;align-items:center;gap:18px}.brand{font-size:27px;font-weight:800;color:var(--ink);text-decoration:none}.brand span{color:var(--blue)}.nav a{font-size:13px;text-decoration:none;margin-left:18px}.nav a.current{font-weight:750}.eyebrow{font-size:11px;letter-spacing:1.3px;text-transform:uppercase;color:var(--blue);font-weight:750;margin:0 0 9px}main{padding:36px 0 70px}h1{font-size:38px;line-height:1.15;letter-spacing:-1px;margin:0 0 12px}h2{font-size:21px;line-height:1.3;margin:0 0 12px}h4{font-size:13px;margin:0 0 10px;letter-spacing:.3px;text-transform:uppercase;color:var(--muted)}.lead{color:var(--muted);max-width:710px}.grid{display:grid;grid-template-columns:330px 1fr;gap:24px;align-items:start;margin-top:30px}.card{background:#fff;border:1px solid var(--line);border-radius:13px;padding:23px;margin-bottom:20px}.field{display:flex;flex-direction:column;gap:6px;margin:0 0 15px}label{font-size:13px;font-weight:700}input,textarea,select{width:100%;border:1px solid #cdd5e1;border-radius:7px;padding:9px 11px;color:var(--ink);background:#fff}textarea{min-height:100px;resize:vertical}.button{border:0;border-radius:7px;background:var(--blue);color:#fff;font-weight:700;padding:10px 14px}.button.secondary{background:#fff;color:var(--ink);border:1px solid var(--line)}.button.small{padding:7px 11px;font-size:12px}.actions{display:flex;gap:10px;flex-wrap:wrap;align-items:center}.status{font-size:12px;color:var(--muted)}.status.error{color:#a12a23}.status.success{color:#27612c}.project{border-top:1px solid var(--line);padding:22px 0}.project:first-child{border-top:0;padding-top:0}.project-top{display:flex;justify-content:space-between;gap:15px;align-items:start}.project h3{font-size:18px;margin:0 0 3px}.badge{display:inline-flex;padding:4px 8px;border-radius:6px;background:#edf2ff;color:var(--blue);font-size:11px;font-weight:700;white-space:nowrap}.badge.preparing{background:#fff3d7;color:#7b5805}.badge.complete{background:#ebf6e8;color:#35652c}.meta{font-size:12px;color:var(--muted);margin:0 0 12px}.summary{font-size:13px;color:var(--muted);white-space:pre-wrap;margin:0 0 14px}.stats{display:flex;gap:20px;flex-wrap:wrap;margin:0 0 17px}.stat{font-size:12px;color:var(--muted)}.stat strong{display:block;font-size:17px;color:var(--ink)}.project-form,.project-panel{border-top:1px solid var(--line);padding-top:17px;margin-top:17px}.project-form summary,.project-panel summary{cursor:pointer;font-weight:700;font-size:13px}.project-form-grid{display:grid;grid-template-columns:1fr 1fr;gap:0 14px;margin-top:16px}.full{grid-column:1/-1}.brief-readonly{background:var(--canvas);border:1px solid var(--line);border-radius:7px;padding:11px;font-size:12px;color:var(--muted);white-space:pre-wrap;margin:0 0 15px}.output{border:1px solid var(--line);border-radius:9px;padding:15px;margin-top:13px}.output-top{display:flex;justify-content:space-between;gap:12px;align-items:start}.output h5{font-size:14px;margin:0 0 3px}.output .meta{margin:0}.submission{background:var(--canvas);border:1px solid var(--line);border-radius:7px;padding:12px;margin:12px 0 0;font-size:13px}.submission p{margin:7px 0 0;white-space:pre-wrap;overflow-wrap:anywhere}.review-actions{display:flex;gap:9px;flex-wrap:wrap;align-items:center;margin-top:13px}.review-actions form{display:contents}.roster{list-style:none;margin:13px 0 0;padding:0;font-size:12px;color:var(--muted);display:flex;gap:14px;flex-wrap:wrap}.notes{font-size:12px;color:var(--muted);background:var(--canvas);border:1px solid var(--line);border-radius:7px;padding:11px;margin-top:14px;white-space:pre-wrap}.empty{border:1px dashed #cbd4e1;border-radius:10px;padding:30px;color:var(--muted);text-align:center}@media(max-width:800px){.grid{grid-template-columns:1fr}.project-form-grid{grid-template-columns:1fr}.full{grid-column:auto}}`;
const script = String.raw`document.querySelectorAll('form[data-api]').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const status=form.querySelector('.status');const buttons=Array.from(form.querySelectorAll('button'));buttons.forEach(b=>b.disabled=true);status.className='status';status.textContent='Saving…';const payload={};for(const field of form.querySelectorAll('[name]'))payload[field.name]=field.type==='number'?Number(field.value):field.value;if(form.dataset.outputs&&typeof payload.deliverables==='string')payload.deliverables=payload.deliverables.split(/\n+/).map(v=>v.trim()).filter(Boolean);if(form.dataset.seed)payload.seedAnax=true;try{const r=await fetch(form.dataset.api,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||'Could not save this change.');status.className='status success';status.textContent='Saved. Reloading…';location.reload()}catch(err){status.className='status error';status.textContent=err.message;buttons.forEach(b=>b.disabled=false)}}));`;

function reviewForm(projectId, task, action, label, secondary) {
  return `<form data-api="/api/team/projects/${esc(projectId)}/tasks/${esc(task.id)}"><input type="hidden" name="action" value="${esc(action)}"><input type="hidden" name="submissionId" value="${esc(task.submission?.id || '')}"><button class="button small${secondary ? ' secondary' : ''}" type="submit">${esc(label)}</button><span class="status" role="status"></span></form>`;
}

function outputCard(project, task) {
  const link = safeLink(task.submission?.url);
  const submitted = task.status === 'submitted';
  const done = task.status === 'done';
  const evidence = task.submission && (link || task.submission.note)
    ? `<div class="submission"><strong>Submitted output</strong>${task.submission.submittedAt ? ` <span class="meta">${esc(stamp(task.submission.submittedAt))}</span>` : ''}${link ? `<p><a href="${esc(link)}" target="_blank" rel="noopener noreferrer">Open submitted work ↗</a></p>` : ''}${task.submission.note ? `<p>${esc(task.submission.note)}</p>` : ''}</div>`
    : submitted || done ? '<div class="submission"><strong>Submitted output</strong><p>No link or note was recorded with this submission.</p></div>' : '';
  const actions = submitted
    ? `<div class="review-actions">${reviewForm(project.id, task, 'approve', 'Approve output')}${reviewForm(project.id, task, 'reopen', 'Request changes', true)}</div>`
    : done ? `<div class="review-actions">${reviewForm(project.id, task, 'reopen', 'Reopen for changes', true)}</div>` : '';
  return `<article class="output"><div class="output-top"><div><h5>${esc(task.title)}</h5><p class="meta">${task.assigneeId ? `Assigned to ${esc(task.assigneeName || 'a project member')}` : 'Not assigned yet'}</p></div><span class="badge${submitted ? ' preparing' : done ? ' complete' : ''}">${esc(outputLabel(task.status))}</span></div>${evidence}${actions}</article>`;
}

function projectCard(project, campuses) {
  const preparing = project.status === 'preparing';
  const tasks = Array.isArray(project.tasks) ? project.tasks : [];
  const allocations = Array.isArray(project.allocations) ? project.allocations : [];
  const awaiting = tasks.filter(task => task.status === 'submitted').length;
  // A non-preparing project submits only its status: echoing an unchanged brief back would
  // read as a brief edit on a project whose scope is already agreed.
  const briefFields = preparing
    ? `<div class="field full"><label for="summary-${esc(project.id)}">Brief</label><textarea id="summary-${esc(project.id)}" name="summary" maxlength="4000">${esc(project.summary)}</textarea></div><div class="field full"><label for="outputs-${esc(project.id)}">Agreed outputs, one per line</label><textarea id="outputs-${esc(project.id)}" name="deliverables" maxlength="30000" placeholder="One deliverable per line">${esc((project.deliverables || []).join('\n'))}</textarea><p class="status">Changing these outputs replaces the project task list. Only possible while the project is preparing and unstaffed.</p></div>`
    : `<div class="field full"><label>Agreed brief</label><p class="brief-readonly">${esc(project.summary)}</p><p class="status">The brief and outputs are locked once a project leaves preparation.</p></div>`;
  return `<article class="project">
    <div class="project-top"><div><p class="eyebrow">${esc(project.client || 'Project')}</p><h3>${esc(project.title)}</h3></div><span class="badge ${preparing ? 'preparing' : project.status === 'completed' || project.status === 'cancelled' ? 'complete' : ''}">${esc(statusLabel(project.status))}</span></div>
    <p class="meta">${esc(campuses.find(campus => campus.id === project.campusId)?.name || 'University not assigned')} · ${money(project.budgetAud)} · ${fte(project.allocatedFte)} / ${fte(project.targetFte)} FTE allocated</p>
    <p class="summary">${esc(project.summary)}</p>
    <div class="stats"><span class="stat"><strong>${allocations.length}</strong>students committed</span><span class="stat"><strong>${tasks.filter(task => task.status === 'done').length} / ${tasks.length}</strong>outputs signed off</span><span class="stat"><strong>${awaiting}</strong>awaiting your review</span></div>
    ${allocations.length ? `<ul class="roster">${allocations.map(item => `<li>${esc(item.name)} · ${fte(item.fte)} FTE</li>`).join('')}</ul>` : ''}
    <details class="project-panel"${awaiting ? ' open' : ''}><summary>Output register (${tasks.length})${awaiting ? ` · ${awaiting} awaiting review` : ''}</summary>${tasks.length ? tasks.map(task => outputCard(project, task)).join('') : '<p class="status">No outputs yet. Add the agreed outputs while the project is preparing.</p>'}</details>
    <details class="project-form"${preparing ? ' open' : ''}><summary>${preparing ? 'Edit brief and open recruitment' : 'Change project status'}</summary>
      <form data-api="/api/team/projects/${esc(project.id)}"${preparing ? ' data-outputs="true"' : ''}><input type="hidden" name="version" value="${esc(project.version)}"><div class="project-form-grid">${briefFields}<div class="field"><label for="status-${esc(project.id)}">Status</label><select id="status-${esc(project.id)}" name="status">${['preparing','recruiting','active','completed','cancelled'].map(status => `<option value="${status}" ${status === project.status ? 'selected' : ''}>${statusLabel(status)}</option>`).join('')}</select></div></div><div class="actions"><button class="button" type="submit">Save project</button><span class="status" role="status"></span></div></form></details>
    ${project.internalNotes ? `<p class="notes"><strong>Private operator note</strong><br>${esc(project.internalNotes)}</p>` : ''}
  </article>`;
}

export function renderCollectiveAdminHtml({ campuses = [], projects = [], anaxDraft = {} } = {}) {
  const campusOptions = campuses.map(campus => `<option value="${esc(campus.id)}">${esc(campus.name)}</option>`).join('');
  const anax = projects.find(project => project.id === 'anax-pilot');
  const anaxCard = anax
    ? `<div class="card"><h2>Anax founding project</h2><p class="lead">Assigned to ${esc(campuses.find(campus => campus.id === anax.campusId)?.name || 'a university')}. Its brief and participation terms are edited in the project register while it stays in preparation.</p><p class="meta">${esc(anax.client)} · ${money(anax.budgetAud)} · ${esc(statusLabel(anax.status))}</p></div>`
    : `<div class="card"><h2>Anax founding project</h2><p class="lead">The founding project has no university yet. It stays a public preparing teaser until you choose one; assigning it here creates it as a preparing project with the fixed teaser figures.</p><p class="meta">${esc(anaxDraft.client || 'Anax Metals')} · ${money(anaxDraft.budgetAud || 7500)} · Preparing</p><form data-api="/api/team/projects" data-seed="true"><div class="field"><label for="anaxCampus">Host university</label><select id="anaxCampus" name="campusId" required><option value="">Choose a university</option>${campusOptions}</select></div><div class="actions"><button class="button" type="submit">Assign the founding project</button><span class="status" role="status"></span></div></form>${anaxDraft.internalNotes ? `<p class="notes"><strong>Before opening it</strong><br>${esc(anaxDraft.internalNotes)}</p>` : ''}</div>`;
  const projectHtml = projects.length ? projects.map(project => projectCard(project, campuses)).join('') : '<p class="empty">No collective projects yet. Assign a university when the first brief is ready.</p>';
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex"><title>Collective projects | Aigents</title><style>${css}</style></head><body><header><div class="wrap nav"><a class="brand" href="/">aigents<span>.</span></a><nav><a href="/team">Employer inbox</a><a class="current" href="/team/projects">Collective projects</a></nav></div></header><main class="wrap"><p class="eyebrow">Operator workspace</p><h1>University collectives</h1><p class="lead">Prepare briefs privately, assign each project to one university, then open the shared team workspace when the scope is agreed. Student participation is workload planning; it does not create an employment or payment contract. Campus membership is self-declared by each student and is not verified against university records.</p><div class="grid"><section><div class="card"><h2>Start a project</h2><p class="lead">Choose the campus that owns the feed and team.</p><form data-api="/api/team/projects" data-outputs="true"><div class="field"><label for="newCampus">University</label><select id="newCampus" name="campusId" required><option value="">Choose a university</option>${campusOptions}</select></div><div class="field"><label for="newTitle">Title</label><input id="newTitle" name="title" maxlength="180" required></div><div class="field"><label for="newClient">Client</label><input id="newClient" name="client" maxlength="160" required></div><div class="field"><label for="newBudget">Budget (AUD)</label><input id="newBudget" name="budgetAud" type="number" min="0" step="0.01" required></div><div class="field"><label for="newTarget">Target workload (FTE)</label><input id="newTarget" name="targetFte" type="number" min="0.01" step="0.01" value="1" required></div><div class="field"><label for="newSummary">Brief</label><textarea id="newSummary" name="summary" maxlength="4000" required></textarea></div><div class="field"><label for="newOutputs">Outputs, one per line</label><textarea id="newOutputs" name="deliverables" maxlength="30000" placeholder="Leave empty while preparing"></textarea></div><div class="field"><label for="newStatus">Status</label><select id="newStatus" name="status"><option value="preparing">Preparing</option><option value="recruiting">Recruiting</option></select></div><div class="actions"><button class="button" type="submit">Create project</button><span class="status" role="status"></span></div></form></div>${anaxCard}</section><section class="card"><h2>Project register</h2><p class="lead">Only operators can see private notes, team rosters and output evidence here. Approving an output signs off the exact submission shown; if a student replaces it first, the review is refused so you can read the new version.</p>${projectHtml}</section></div></main><script>${script}</script></body></html>`;
}
